# VIDE CODING

## Claude Code 시스템 프롬프트

이미 박혀 있는 안전장치들

```system prompt
Never run force push to main/master

Never use git commands with -i flag

Always create NEW commits rather than amending
```

여러분이 claude code 를 실행하는 순간, 이 하네스가 자동으로 작동하고 있었습니다
